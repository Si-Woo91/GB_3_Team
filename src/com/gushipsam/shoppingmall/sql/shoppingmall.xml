<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shoppingmall">
	<insert id="join" parameterType="shoppingmalldto">
		INSERT INTO MEMBERS VALUES (#{userid,jdbcType=VARCHAR}, #{userpw,jdbcType=VARCHAR}, #{username,jdbcType=VARCHAR}, 
		#{userphone,jdbcType=VARCHAR}, #{userdata,jdbcType=VARCHAR}, #{useremail,jdbcType=VARCHAR})
	</insert>

	<select id="checkId" parameterType="string" resultType="_int">
      SELECT count(*) FROM MEMBERS WHERE USERID = #{userid}
   </select>
   
   <select id="login" parameterType="hashmap" resultType="_int">
		SELECT count(*) FROM MEMBERS WHERE USERID = #{userid} AND USERPW = #{userpw}
	</select>
	
   
</mapper>













